# AI SaaS Business

## Landing Page Builder Deployment

Subdomain deployment setup and domain configuration guide:

- `docs/LANDING_PAGE_SUBDOMAIN_DEPLOY_SETUP.md`

This includes:

- Auto subdomain assignment
- Availability checks
- Custom wildcard domain setup on Vercel
- No-domain fallback mode (`/preview/<subdomain>`)

