# =================================================================
# AI SAAS BUSINESS - ENVIRONMENT CONFIGURATION
# =================================================================
# Copy this file to .env and fill in your values
# Keep .env secure and never commit it to version control!
# =================================================================


# =================================================================
# DATABASE & AUTHENTICATION (Existing)
# =================================================================
MONGO_URI=your_mongodb_connection_string
# Use a long random secret in production
JWT_SECRET=your_jwt_secret_key_here
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your_nextauth_secret_here
# Optional: used by NextAuth callback verification if backend URL differs
NEXT_PUBLIC_BACKEND_URL=http://localhost:3000
# Optional: set in production if cookies should be shared across subdomains
# Example: COOKIE_DOMAIN=.yourdomain.com
COOKIE_DOMAIN=

# Google OAuth
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret
# Comma-separated admin users
# Example: ADMIN_EMAILS=admin@yourdomain.com,owner@yourdomain.com
# Admin panel uses normal account login.
# There is no separate master admin password in env.
# Set an email in ADMIN_EMAILS, then sign in with that account password.
ADMIN_EMAILS=admin@yourdomain.com
# Optional primary admin bootstrap account (first setup)
# Use your real admin email (not placeholder) to avoid 401 on /admin-login.
PRIMARY_ADMIN_EMAIL=admin@yourdomain.com
PRIMARY_ADMIN_PASSWORD=change_this_to_a_strong_password
PRIMARY_ADMIN_NAME=PrimaryAdmin

# Email Configuration
EMAIL_HOST=smtp.example.com
EMAIL_PORT=587
EMAIL_SECURE=false
EMAIL_USER=your_email@example.com
EMAIL_PASSWORD=your_email_password
DEPLOYMENT_NOTIFICATION_TO=ops@yourdomain.com

# Landing Page Builder custom subdomain root (optional)
# Example: NEXT_PUBLIC_ROOT_DOMAIN=yourdomain.com
# If not set, deployed URLs will use /preview/<subdomain> on your current host.
NEXT_PUBLIC_ROOT_DOMAIN=


# =================================================================
# AI APP BUILDER - REQUIRED CONFIGURATION
# =================================================================

# FIRECRAWL - Required for website scraping & cloning
# Get your API key at: https://firecrawl.dev
FIRECRAWL_API_KEY=your_firecrawl_api_key


# =================================================================
# BILLING & PAYMENTS (PayU)
# =================================================================
# PayU credentials for paid plan checkout
PAYU_MERCHANT_KEY=your_payu_merchant_key
PAYU_MERCHANT_SALT=your_payu_merchant_salt
# test or production
PAYU_MODE=test
# Optional: override base URL manually (rarely needed)
# PAYU_BASE_URL=https://test.payu.in


# =================================================================
# AI PROVIDER - Choose at least ONE LLM Provider
# =================================================================
# You need at least one AI provider to use the AI App Builder

# Google Gemini (Recommended - Fast & Cost-effective)
# Get your API key at: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key

# Anthropic Claude (Best for complex reasoning)
# Get your API key at: https://console.anthropic.com
ANTHROPIC_API_KEY=your_anthropic_api_key

# OpenAI GPT (Popular & versatile)
# Get your API key at: https://platform.openai.com
OPENAI_API_KEY=your_openai_api_key

# Groq (Ultra-fast inference)
# Get your API key at: https://console.groq.com
GROQ_API_KEY=your_groq_api_key


# =================================================================
# FAST APPLY - Optional (Faster code edits)
# =================================================================
# MorphLLM provides faster code application
# Get your API key at: https://morphllm.com/dashboard
# MORPH_API_KEY=your_morphllm_api_key


# =================================================================
# SANDBOX PROVIDER - Required for code execution
# =================================================================
# Choose ONE sandbox provider: 'vercel' (recommended) or 'e2b'
SANDBOX_PROVIDER=vercel


# -----------------------------------------------------------------
# Option 1: Vercel Sandbox (Recommended)
# -----------------------------------------------------------------
# Vercel provides fast, reliable sandboxes for running React apps

# Method A: OIDC Token (Automatic - for development)
# Run these commands in your project directory:
#   vercel link
#   vercel env pull
# This will automatically add VERCEL_OIDC_TOKEN to your .env
VERCEL_OIDC_TOKEN=auto_generated_by_vercel_env_pull

# Method B: Personal Access Token (For production or CI/CD)
# Get these from your Vercel dashboard: https://vercel.com/account/tokens
# VERCEL_TEAM_ID=team_xxxxxxxxx
# VERCEL_PROJECT_ID=prj_xxxxxxxxx  
# VERCEL_TOKEN=vercel_xxxxxxxxxxxx


# -----------------------------------------------------------------
# Option 2: E2B Sandbox (Alternative)
# -----------------------------------------------------------------
# E2B provides secure code execution environments
# Get your API key at: https://e2b.dev
# E2B_API_KEY=your_e2b_api_key


# =================================================================
# ADVANCED CONFIGURATION (Optional)
# =================================================================

# NPM Flags for package installation
NPM_FLAGS=--legacy-peer-deps

# Auto restart Vite server after package installation
AUTO_RESTART_VITE=true

# Default AI Model (Options: gemini-2.0-flash, gpt-4o, claude-3-5-sonnet, llama-3-70b)
DEFAULT_AI_MODEL=gemini-2.0-flash


# =================================================================
# CLOUDINARY (Existing - for image uploads)
# =================================================================
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret


# =================================================================
# CONVEX (Existing - if using)
# =================================================================
CONVEX_DEPLOYMENT=your_convex_deployment
NEXT_PUBLIC_CONVEX_URL=your_convex_url


# =================================================================
# SETUP GUIDE - AI APP BUILDER
# =================================================================
#
# 1. MINIMUM REQUIREMENTS:
#    - At least ONE AI provider (Gemini recommended)
#    - ONE sandbox provider (Vercel recommended)
#    
# 2. RECOMMENDED SETUP (Fastest to get started):
#    a. Sign up at https://aistudio.google.com and get GEMINI_API_KEY
#    b. Run `vercel link` in your project, then `vercel env pull`
#    c. Set SANDBOX_PROVIDER=vercel
#    d. (Optional) Get FIRECRAWL_API_KEY for website cloning
#
# 3. FOR WEBSITE CLONING FEATURE:
#    - Sign up at https://firecrawl.dev
#    - Add your FIRECRAWL_API_KEY
#
# 4. TESTING YOUR SETUP:
#    - npm run dev
#    - Navigate to /generation
#    - Try generating a simple React app
#
# =================================================================
